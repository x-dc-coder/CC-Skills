[project]
name = "claude-skills"
version = "0.1.0"
description = "Unified Python environment for Claude Code skills"
requires-python = ">=3.10"
dependencies = [
    "Pillow>=10.0.0",
    "sqlglot>=20.0.0",
]

[tool.uv]
package = false
